Git – это распределенная система контроля версий.
Распределенность git’а отличает его от прочих vcs. Распределенность - возможность использования одной системы контроля на проекте множеством разработчиков.
Это значит, что мы в любой момент времени можем склонировать локально на компьютер и работать с ним на нашей машине.
Это система, которая позволяет нам хранить всю историю изменений, которые мы когда-либо вносили в проект.

Что еще умеет Git?
Хранит всю историю разработки;
Права доступа к коду;
Контроль версий(кто, когда и что вносил).

Репозиторий - это весь проект в Git (место, где хранятся и поддерживаются какие-либо данные)

Конфликты!
Конфликты возникают, когда два человека изменяют одни и те же строки в файле или один разработчик удаляет файл, который в это время изменяет другой разработчик.
Конфликты затрагивают только того разработчика, который выполняет слияние, остальная часть команды о конфликте не знает. Git помечает файл как конфликтующий и останавливает процесс слияния. 